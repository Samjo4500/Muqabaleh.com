/** Nested dashboard routes inherit the parent interviewer atelier shell. */
export default function InterviewerDashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
